// Action 提交的是 mutation，而不是直接变更状态。
// Action 可以包含任意异步操作。
import axios from 'axios'

/* 导出actions方法 */
// import { showLoading } from './widgetUi'

function test({ state, commit, dispatch }, obj) {
    console.log(test)
}
export default {
    // updateRefreshState({ commit }) {
    //     commit('updateRefreshState')
    // },
    // showLoading
}