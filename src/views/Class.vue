<template>
  <div>

  </div>
</template>

<script>
  /* class Cat { 
  constructor(name) {//构造
    this.name = name;
  }
  speak() {//方法
      console.log(this.name + ' makes a noise.');
    }
  }
  class Lion extends Cat {
    speak() {
      super.speak();
      console.log(this.name + ' roars.');
    }
  }
  class Rectangle {
    // constructor
    constructor(height, width) {
      this.log = [];
      this.height = height;
      this.width = width;
    }
    // Getter
    get area() {
      return this.calcArea()
    }
    // Setter
    set width(width){
      this.log.push({width:width});
    }
    set height(height){
      this.log.push({height:height});
    }
    // Method
    calcArea() {
        return this.height * this.width;
    }
  }
  const square = new Rectangle(10, 10);
  square.width=20;
  console.log(square); */

</script>

<style scoped>

</style>